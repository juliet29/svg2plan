from __init__ import *
from svg_logger.settings import svlogger
from runner.svg2plan import semi_run
from new_solutions.simple_problem import run






if __name__ == "__main__":
    semi_run()
    # re = run()
    # re = run_new_layout()
    # save_new_layout(re)
    # plot(re)

